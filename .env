# Base URL of the CareerConnect backend API
# Leave unset (or comment out) to use a relative "/api" path via the Vite
# dev proxy (see vite.config.js) or a production reverse proxy.
# VITE_API_URL=http://localhost:5000/api
VITE_API_URL=http://15.135.189.44:5000/api
